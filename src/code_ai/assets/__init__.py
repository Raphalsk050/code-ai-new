"""Packaged, read-only assets shipped with Code-AI.

Currently holds ``default_skills/`` — the skills seeded into the user's
``~/.code-ai/skills`` on first run (see ``code_ai.tools.skills.seed``).
"""
